# TODO

## Milestones

---

### 1. Interrupt subsystem

* GPIO interrupts
* PCNT interrupts
* Register PHP fnctions as interrupt handlers
* Event-based dispatch

### 2. VM Yield subsystem

* Ability to pause and resume any VM regardless of its current state
* Add `yield()` support for PHP scripts
* Allow multiple VMs to run within a single FreeRTOS task context

### 3. ESP-IDF bindings

* Implement a selected subset of ESP-IDF APIs (GPIO API first)


### 4. Arduino Core bindings
  * Almost done

### 5. Standard Library

An analogue of the Arduino Core `.cpp` library, implemented in PHP where possible.

Standard Arduino Core classes:

* `HardwareSerial`
* `SPI`
* `FS`
* `Print`
* `Server`
* etc.

So far Print, Stream, Server, Client are implemented
---

## Smaller tasks

---

### 1. `resource` type

Add the `resource` type and alias it to `uint64`.

```php
resource $var = fopen();
```

### 2. `enum`

Add the `enum` keyword.

Implement enums as classes, so `Class::field` syntax can be used naturally.

Consider enum casting semantics:

* pure enum
* `key => value` enum  (should we define our own syntax, C-like?)

```php
enum Suit
{
    case Hearts;
    case Diamonds;
    case Clubs;
    case Spades;
}

function do_stuff(Suit $s)
{
    // ...
}

do_stuff(Suit::Spades);
```

### 3. `match` keyword

### 4. Short array syntax

Support:

```php
$arr = [];
```

lex.c and compile.c must be updated

### 5. function_exists()

Implement `function_exists()`.
